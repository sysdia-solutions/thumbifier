defmodule Thumbifier.Convert.Processor do
  def process(data) do
    IO.inspect data
    # fetch media url and save local copy
    # get mime type of temp file
    # pass file details to strategy
    # pass converted temp file to jpg strategy
    # get final jpg temp file as base64 string
    # post response_id, personal_reference, image base64 string to callback url
  end
end
